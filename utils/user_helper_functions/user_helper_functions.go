package userhelperfunctions

// func GetRentalFromApartmentList(allrentals []usermodels.PropertyModel, rentalName string) (usermodels.PropertyModel, error) {
// 	var est usermodels.PropertyModel
// 	for _, v := range allrentals {
// 		if v.PropertyName == rentalName {
// 			return v, nil
// 		}

// 	}
// 	return est, fmt.Errorf("no rental with such name")

// }

// func UpdateRentalEstateDetails(user usermodels.UserModel, newrental usermodels.PropertyModel, rentalname string) usermodels.UserModel {

// 	for i, v := range user.EstateApartments {
// 		if v.ApartmentName == rentalname {
// 			user.EstateApartments[i] = newrental

// 		}

// 	}
// 	return user

// }
